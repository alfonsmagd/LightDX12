# Original glTF test scene

Two pyramids and a floor, generated specifically for Ldx12 under the repository's MIT license. No external model is used.

`GenerateScene.py` regenerates the external-buffer, base64-buffer and GLB variants with Python's standard library. The fixture exercises indexed and non-indexed meshes, missing normals, material colors, parent transforms and mirrored nonuniform scale.
